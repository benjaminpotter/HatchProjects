Set the background to 221,194,150
Create a draw function


	Set the background to 221,194,150
	Create an if statement that triggers only when mouseX is greater than 200
	
		Draw an ellipse at position random(0,400),random(0,400) with size 20,20
	
	
	Create an if statement that triggers only when mouseX is less than 200
	
		Draw a rectangle at position random(0,400),random(0,400) with size 20,20
